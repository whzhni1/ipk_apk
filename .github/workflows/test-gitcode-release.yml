name: 测试 Release 脚本

on:
  workflow_dispatch:
    inputs:
      platform:
        description: '测试平台'
        required: true
        type: choice
        options:
          - 'all'
          - 'gitcode'
          - 'gitee'
          - 'gitlab'
        default: 'all'
      
      tag_name:
        description: 'Release 标签名'
        required: true
        default: 'v1.0.0-test'
      
      release_title:
        description: 'Release 标题'
        required: true
        default: '测试 Release'
      
      enable_version_write:
        description: '写入版本到 version-manager.sh'
        required: true
        type: boolean
        default: false

env:
  # 平台配置
  GITCODE_REPO: test-release
  GITEE_REPO: test-release
  GITLAB_REPO: test-release
  
  GITCODE_USERNAME: whzhni
  GITEE_USERNAME: whzhni
  GITLAB_USERNAME: whzhni
  
  BRANCH: main

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      platforms: ${{ steps.set-matrix.outputs.platforms }}
    steps:
      - name: 设置测试平台矩阵
        id: set-matrix
        run: |
          if [ "${{ github.event.inputs.platform }}" = "all" ]; then
            echo "platforms=[\"gitcode\",\"gitee\",\"gitlab\"]" >> $GITHUB_OUTPUT
          else
            echo "platforms=[\"${{ github.event.inputs.platform }}\"]" >> $GITHUB_OUTPUT
          fi
      
      - name: 显示测试配置
        run: |
          echo "### 🧪 测试配置" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ github.event.inputs.platform }}" = "all" ]; then
            echo "- **测试平台**: GitCode, Gitee, GitLab (全部)" >> $GITHUB_STEP_SUMMARY
          else
            echo "- **测试平台**: ${{ github.event.inputs.platform }}" >> $GITHUB_STEP_SUMMARY
          fi
          echo "- **标签名称**: ${{ github.event.inputs.tag_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Release 标题**: ${{ github.event.inputs.release_title }}" >> $GITHUB_STEP_SUMMARY
          echo "- **版本写入**: ${{ github.event.inputs.enable_version_write }}" >> $GITHUB_STEP_SUMMARY

  test-release:
    needs: prepare
    runs-on: ubuntu-latest
    strategy:
      matrix:
        platform: ${{ fromJson(needs.prepare.outputs.platforms) }}
      fail-fast: false
    
    name: 测试 ${{ matrix.platform }}
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
      
      - name: 创建测试文件
        run: |
          echo "构建时间: $(date)" > build-time.txt
          echo "版本: ${{ github.event.inputs.tag_name }}" > version.txt
          echo "# 测试 Release 文件" > README.md
          echo "此文件用于测试 Release 上传功能" >> README.md
      
      - name: 写入版本信息
        if: github.event.inputs.enable_version_write == 'true'
        run: |
          if [ -f "version-manager.sh" ]; then
            chmod +x version-manager.sh
            ./version-manager.sh set "${{ github.event.inputs.tag_name }}"
            echo "✅ 已写入版本: ${{ github.event.inputs.tag_name }}" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ version-manager.sh 不存在" >> $GITHUB_STEP_SUMMARY
          fi
      
      - name: 测试 GitCode Release
        if: matrix.platform == 'gitcode'
        env:
          GITCODE_TOKEN: ${{ secrets.GITCODE_TOKEN }}
          USERNAME: ${{ env.GITCODE_USERNAME }}
          REPO_NAME: ${{ env.GITCODE_REPO }}
          TAG_NAME: ${{ github.event.inputs.tag_name }}
          RELEASE_TITLE: ${{ github.event.inputs.release_title }}
          RELEASE_BODY: "GitCode 单平台测试"
          UPLOAD_FILES: "build-time.txt version.txt README.md"
        run: |
          chmod +x .github/workflows/scripts/release-gitcode.sh
          ./.github/workflows/scripts/release-gitcode.sh
      
      - name: 测试 Gitee Release
        if: matrix.platform == 'gitee'
        env:
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
          USERNAME: ${{ env.GITEE_USERNAME }}
          REPO_NAME: ${{ env.GITEE_REPO }}
          TAG_NAME: ${{ github.event.inputs.tag_name }}
          RELEASE_TITLE: ${{ github.event.inputs.release_title }}
          RELEASE_BODY: "Gitee 单平台测试"
          UPLOAD_FILES: "build-time.txt version.txt README.md"
        run: |
          chmod +x .github/workflows/scripts/release-gitee.sh
          ./.github/workflows/scripts/release-gitee.sh
      
      - name: 测试 GitLab Release
        if: matrix.platform == 'gitlab'
        env:
          GITLAB_TOKEN: ${{ secrets.GITLAB_TOKEN }}
          USERNAME: ${{ env.GITLAB_USERNAME }}
          REPO_NAME: ${{ env.GITLAB_REPO }}
          TAG_NAME: ${{ github.event.inputs.tag_name }}
          RELEASE_TITLE: ${{ github.event.inputs.release_title }}
          RELEASE_BODY: "GitLab 单平台测试"
          UPLOAD_FILES: "build-time.txt version.txt README.md"
        run: |
          chmod +x .github/workflows/scripts/release-gitlab.sh
          ./.github/workflows/scripts/release-gitlab.sh

  summary:
    needs: [prepare, test-release]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: 生成测试报告
        run: |
          echo "# 📊 测试结果汇总" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 测试配置" >> $GITHUB_STEP_SUMMARY
          echo "- **平台**: ${{ github.event.inputs.platform }}" >> $GITHUB_STEP_SUMMARY
          echo "- **标签**: ${{ github.event.inputs.tag_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **版本写入**: ${{ github.event.inputs.enable_version_write }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 执行状态" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ needs.test-release.result }}" = "success" ]; then
            echo "✅ 所有平台测试通过" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ 部分平台测试失败，请查看详细日志" >> $GITHUB_STEP_SUMMARY
          fi
